build:
  docker:
    web: Dockerfile
  config:
  
    DB_USER: postgres
    DB_PASS: y3ND_dB8#jP3GAy
    DB_HOST: db.rmagpeekgmsbbtlpzatl.supabase.co
    DB_PORT: 5432
    DB_NAME: postgres
    FRONTEND_URL: https://health-score-dashboard.herokuapp.com

release:
  command:
    - npm run migrate
    - npm run seed

run:
  web: npm run start
